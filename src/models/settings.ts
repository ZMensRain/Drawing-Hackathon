type settings = {
  theme: "light" | "dark";
  brushSize: number;
  brushColor: string;
  tool: number;
  dysfunctionalRange: number[];
  backgroundColor: string;
  popups: boolean;
};

export type { settings };

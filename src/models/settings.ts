type settings = {
  theme: "light" | "dark";
  brushSize: number;
  brushColor: string;
  tool: number;
  dysfunctionalModifier: number;
  backgroundColor: string;
  popups: boolean;
};

export type { settings };
